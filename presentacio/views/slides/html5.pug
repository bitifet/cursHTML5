
.slide.bigfont(data-name="html5_is_here")
  div

    h2.step.title
      span HTML
      img.inline(src="/images/html5.png")

    table.step
      tr
        th HTML 5
        td.code
          code='<!DOCTYPE HTML>'


.slide(data-name="ecosistema")
  div

    header.step.fade
      h2
        span HTML
        img.inline(src="/images/html5.png")
      h3 Ecosistema

    table
      tr.step
        th.lateral Continguts
        td Markup (HTML)
      tr.step
        th.lateral Aspecte
        td Estils (CSS)
      tr.step
        th.lateral Lògica d'Aplicació
        td Javascript + DOM
      tr.step
        th.lateral Interacció
        td APIs HTML5


.slide(data-name="elements")

  div
    header.step.fade
      h2
        span HTML
        img.inline(src="/images/html5.png")
      h3 Markup - Elements

    ul
      li.step.fade Amb contingut:

        pre.step.
          &lt;div class="menu"&gt;
              (contingut...)
          &lt;/div&gt;

      li.step.fade Auto-tancats:

        pre.step.
          &lt;input name="age" type="number" /&gt;

      li.step.fade Custom Elements:

        pre.step.
          &lt;my-component&gt;
              (contingut...)
          &lt;/my-component&gt;


.slide(data-name="atributs")

  div
    header.step.fade
      h2
        span HTML
        img.inline(src="/images/html5.png")
      h3 Markup - Atributs

    ul
      li.step Els noms del atributs és "case-insensitive" a HTML5.
      li.step Atributs personalitzats: #[code.code data-myCoolAttr="value"].


.slide(data-name="estructura")

  div
    header.step.fade
      h2
        span HTML
        img.inline(src="/images/html5.png")
      h3 Markup - Estructura

    ul
      li.step DTD
      li.step Document (#[code.code &lt;html&gt;])
        ul
          li.step Capçalera (#[code.code &lt;head&gt;])
          li.step Cos (#[code.code &lt;body&gt;])



.slide(data-name="heading_elements")

  div
    header.step.fade
      h2
        span HTML
        img.inline(src="/images/html5.png")
      h3 Markup - Elements de la Capçalera

    ul
      li.step Títol (#[code.code &lt;title&gt;])
      li.step Codificació de caràcters: #[code.code &lt;meta charset='utf-8' /&gt;]
      li.step Viewport: #[code.code &lt;meta name="viewport" content="width=device-width, initial-scale=1"&gt;]
      li.step Fulls d'estil
        ul
          li.step Externs: #[code.code &lt;link rel="stylesheet" type="text/css" href="..." /&gt;]
          li.step Interns: #[code.code &lt;style&gt;...&lt;/style&gt;] (També poden anar dins el cos)
      li.step Scripts (?)
        ul
          li.step Externs: #[code.code &lt;script src="..." /&gt;] (També poden anar dins el cos#[span.super *])
          li.step Interns: #[code.code &lt;script&gt;...&lt;/script&gt;] (També poden anar dins el cos#[span.super *])




.slide(data-name="body_tags")

  div
    header.step.fade
      h2
        span HTML
        img.inline(src="/images/html5.png")
      h3 Markup - Elements del Cos

    img.big.step.fade(src="/images/figures/html_tags.jpg")


.slide(data-name="new_elements")

  div
    header.step.fade
      h2
        span HTML
        img.inline(src="/images/html5.png")
      h3 Markup - Elements Nous a HTML5


    ul
      li.step #[code.code &lt;header&gt;&lt;/header&gt;]
      li.step #[code.code &lt;footer&gt;&lt;/footer&gt;]
      li.step #[code.code &lt;section&gt;&lt;/section&gt;]
      li.step #[code.code &lt;article&gt;&lt;/article&gt;]
      li.step #[code.code &lt;aside&gt;&lt;/aside&gt;]
      li.step etcètera...

    p.moreinfo.step https://www.w3schools.com/html/html5_new_elements.asp


//- vim: set ft=jade:
